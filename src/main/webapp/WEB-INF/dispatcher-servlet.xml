<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- ======================================== -->
    <!-- 0. application.properties 로딩            -->
    <!-- ======================================== -->
    <context:property-placeholder location="classpath:application.properties"/>

    <!-- ======================================== -->
    <!-- 1. 컴포넌트 스캔                          -->
    <!-- ======================================== -->
    <context:component-scan base-package="com.hgu.guitar"/>

    <!-- ======================================== -->
    <!-- 2. 정적 리소스                            -->
    <!-- ======================================== -->
    <!-- 프로젝트 리소스 -->
    <mvc:resources mapping="/resources/**" location="/resources/"/>

    <!-- 업로드 파일: webapp/upload 아래 -->
    <mvc:resources mapping="/upload/**" location="/upload/"/>

    <!-- ✅ 중요: Dispatcher가 / 로 먹는 정적요청을 톰캣 기본서블릿에 위임 -->
    <mvc:default-servlet-handler/>

    <!-- ======================================== -->
    <!-- 3. Spring MVC 기본 설정                   -->
    <!-- ======================================== -->
    <mvc:annotation-driven/>

    <!-- ======================================== -->
    <!-- 4. JSP ViewResolver                      -->
    <!-- ======================================== -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- ======================================== -->
    <!-- 5. DB 연결(DataSource)                   -->
    <!-- ======================================== -->
    <bean id="dataSource"
          class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <!-- ======================================== -->
    <!-- 6. MyBatis SqlSessionFactory              -->
    <!-- ======================================== -->
    <bean id="sqlSessionFactory"
          class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:mybatis/mybatis-config.xml"/>
        <property name="mapperLocations" value="classpath:mybatis/mapper/*.xml"/>
    </bean>

    <!-- ======================================== -->
    <!-- 7. Mapper 자동 스캔                       -->
    <!-- ======================================== -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.hgu.guitar.dao"/>
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"/>
    </bean>

    <!-- ======================================== -->
    <!-- 8. 파일 업로드 설정                       -->
    <!-- ======================================== -->
    <bean id="multipartResolver"
          class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="UTF-8"/>
        <property name="maxUploadSize" value="10485760"/>
    </bean>

    <!-- ======================================== -->
    <!-- 9. 인증 인터셉터 Bean 등록                -->
    <!-- ======================================== -->
    <bean id="authInterceptor"
          class="com.hgu.guitar.interceptor.AuthInterceptor"/>

    <!-- ======================================== -->
    <!-- 10. 인터셉터 URL 정책                     -->
    <!-- ======================================== -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**"/>

            <!-- 비회원 허용 -->
            <mvc:exclude-mapping path="/"/>
            <mvc:exclude-mapping path="/list/**"/>

            <!-- 로그인/회원가입 -->
            <mvc:exclude-mapping path="/login/**"/>

            <!-- 정적 리소스 -->
            <mvc:exclude-mapping path="/resources/**"/>
            <mvc:exclude-mapping path="/upload/**"/>

            <ref bean="authInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

</beans>
